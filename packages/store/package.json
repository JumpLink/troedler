{
  "name": "@troedler/store",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local state: XDG paths, the config manifest, saved searches, the seen index and the price history. Uses node:* only — never a provider package, not even transitively — so the watch logic is unit-testable on Node against :memory:.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "check": "tsc --noEmit --project tsconfig.json",
    "lint": "gjsify lint ."
  },
  "files": ["src"],
  "dependencies": {
    "@troedler/core": "*"
  }
}
